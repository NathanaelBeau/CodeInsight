In dataframe df0, conditionally fill values in column colB based on the values in another column colA being equal to some_value with the new_value 
import pandas as pd
def test(df0, colA, some_value, colB, new_value):